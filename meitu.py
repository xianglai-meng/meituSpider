#coding='utf-8'
import re


historyList


def GetUrlList(html):
	AimUrl.deep+=1
 return list[AimUrl]

def GetAimContent(url):



class AimUrl(object):

url=''
deep =0
mianUrl=''


def main

mianUrl = ''

html= urlLib.urlopen(mianUrl)

urlList =  GetUrlList(html)

for url in urlList:
	if urlList.deep<3:
		if historyList.lenght==0:
			GetAimContent(url)
			historyList.append(url)
		else:
			isRepeat = historyList.find(url)
			if !isRepeat:
				GetAimContent(url)
				historyList.append(url)
